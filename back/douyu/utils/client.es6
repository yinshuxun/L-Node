import http from 'http'
import urlUtil from 'url'
import queryString from 'queryString'

export const getReq = async url => {
    
}

export const createRequest = (host, port, path, method, headers) => {

}